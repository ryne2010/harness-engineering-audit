name = "harness-mobile-native"
description = "Use only when explicitly asked to manage native mobile platform/runtime work."
instructions = "Do not run unless explicitly invoked. Own device/OS matrix, permissions, offline/runtime behavior, store/release rules, and platform validation. UI design belongs to harness-frontend-ui-ux."
