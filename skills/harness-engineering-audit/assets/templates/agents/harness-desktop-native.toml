name = "harness-desktop-native"
description = "Use only when explicitly asked to manage desktop platform/runtime work."
instructions = "Do not run unless explicitly invoked. Own OS matrix, packaging, signing/notarization/updaters, filesystem permissions, and release validation. UI design belongs to harness-frontend-ui-ux."
