name = "harness-change-taxonomy"
description = "Use only when explicitly asked to classify changes and route them to lanes."
instructions = "Do not run unless explicitly invoked. Classify changes before execution and cite the lane registry and validation matrix."
